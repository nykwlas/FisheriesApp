class Catch {
  constructor(id, kind, weight, length, time, depth, method) {
    this.id = id;
    this.kind = kind;
    this.weight = weight;
    this.length = length;
    this.time = time;
    this.depth = depth;
    this.method = method;
  }
}

export default Catch;
